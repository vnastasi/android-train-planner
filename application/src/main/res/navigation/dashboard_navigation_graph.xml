<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dashboard_navigation_graph"
    app:startDestination="@id/trip_planner">

    <fragment
        android:id="@+id/trip_planner"
        android:name="md.vnastasi.trainplanner.dashboard.planner.TripPlannerFragment"
        android:label="trip_planner"
        tools:layout="@layout/fragment_trip_planner" />

    <fragment
        android:id="@+id/map_fragment_container"
        android:name="md.vnastasi.trainplanner.dashboard.map.MapFragment"
        android:label="map"
        tools:layout="@layout/fragment_maps" />

    <fragment
        android:id="@+id/disruptions"
        android:name="md.vnastasi.trainplanner.dashboard.disruption.ui.DisruptionListFragment"
        android:label="disruption_list"
        tools:layout="@layout/fragment_disruption_list" />

</navigation>